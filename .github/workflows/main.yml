name: Hello

on:
  push:
    branches:
      - main

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello to user
        run: echo "Hello dosto"

      - name: Hello user
        run: echo "Hello $USER"
